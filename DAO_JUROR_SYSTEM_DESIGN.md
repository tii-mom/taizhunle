# 太准了 - 陪审员系统完整设计

## 📋 系统概览

陪审员是太准了预测市场的核心治理角色，负责对有争议的预测结果进行仲裁投票。

## 🎯 陪审员生命周期

### 1. 申请成为陪审员

#### 准入条件
- **最低质押**：1,000 TAI
- **钱包绑定**：已连接 TonConnect 钱包
- **Telegram 绑定**：已绑定 Telegram Bot（用于接收任务通知）

#### 申请流程
```
1. 用户点击"成为陪审员"
2. 系统检查质押余额（≥1,000 TAI）
3. 用户确认质押金额
4. 签名交易，质押 TAI 到 DAO 池
5. 质押成功，获得陪审员资格
6. 初始积分：0 分
```

#### 质押说明
- 质押的 TAI 锁定在 DAO 池中
- 可以追加质押（提高抽签权重）
- 退出陪审需要 7 天解锁期
- 解锁期间不能参与新任务，但可以完成已分配任务


### 2. 任务分配机制

#### 抽签规则
- **每日抽签次数**：最多 4 次
- **抽签时间**：每 6 小时一轮（00:00, 06:00, 12:00, 18:00 UTC）
- **抽签权重**：`(积分 + 10) × 质押额`
- **任务数量**：每轮根据待审预测数量动态分配

#### 权重计算示例
```
陪审员 A：积分 50，质押 5,000 TAI
权重 = (50 + 10) × 5,000 = 300,000

陪审员 B：积分 10，质押 10,000 TAI  
权重 = (10 + 10) × 10,000 = 200,000

陪审员 A 被抽中概率更高
```

#### 任务类型
1. **争议预测**：双方投票接近，需要陪审团裁决
2. **举报预测**：用户举报预测结果有误
3. **超时预测**：创建者未及时设置结果

#### 任务状态
- `pending` - 待处理（刚分配）
- `in_progress` - 处理中（已查看）
- `voted` - 已投票
- `completed` - 已完成（结果已确定）
- `expired` - 已过期（未在规定时间内投票）


### 3. 投票流程

#### 投票时限
- **标准任务**：48 小时
- **紧急任务**：24 小时
- **超时处理**：视为弃权，不影响结果但不获得奖励

#### 投票选项
- **选项 A 胜出**：支持 A 方获胜
- **选项 B 胜出**：支持 B 方获胜
- **预测无效**：认为预测本身有问题（如描述不清、作弊等）

#### 投票权重
- 每个陪审员 1 票
- 简单多数决（>50%）
- 如果"预测无效"获胜，全额退款给所有参与者

#### 投票证据
- 陪审员可以查看：
  - 预测详情和规则
  - 双方投票数据
  - 举报理由（如有）
  - 相关证据链接
- 陪审员需要提供：
  - 投票选择
  - 投票理由（可选，但推荐）


### 4. 奖励机制

#### 基础奖励
- **奖励来源**：预测市场手续费的 1%
- **分配方式**：按参与陪审的人数平分
- **发放时间**：预测结算后立即到账 DAO 池

#### 奖励计算示例
```
预测总池：1,000,000 TAI
手续费 2.5%：25,000 TAI
陪审员奖励 1%：10,000 TAI

参与陪审员：5 人
每人获得：10,000 / 5 = 2,000 TAI
```

#### 积分奖励
- **投票完成**：+1 积分
- **站对多数派**：+1 积分（共 +2）
- **站错少数派**：+0 积分（仅 +1 基础分）

#### 额外奖励
- **连续参与奖励**：连续 7 天参与，额外 +5 积分
- **高质量投票**：提供详细理由且被采纳，额外 +2 积分
- **快速响应**：12 小时内完成投票，奖励 +10% TAI


### 5. 惩罚机制（待实施）

#### 惩罚场景

**A. 恶意投票**
- **定义**：明显违背事实的投票
- **检测**：社区举报 + 管理员审核
- **惩罚**：
  - 扣除 10% 质押金（销毁）
  - 扣除 20 积分
  - 暂停陪审资格 7 天

**B. 长期不参与**
- **定义**：连续 3 次被抽中但未投票
- **惩罚**：
  - 扣除 5 积分
  - 降低抽签权重 50%（持续 7 天）

**C. 串通作弊**
- **定义**：多个陪审员协同作弊
- **检测**：投票模式分析 + 社区举报
- **惩罚**：
  - 扣除 50% 质押金（销毁）
  - 永久取消陪审资格
  - 公示作弊记录

**D. 随意投票**
- **定义**：投票准确率长期低于 30%
- **惩罚**：
  - 警告提示
  - 降低抽签权重 30%
  - 建议学习陪审规则


#### 申诉机制
- 被惩罚的陪审员可以提交申诉
- 申诉需要提供证据和说明
- 由高级陪审员（积分 >200）投票决定
- 申诉成功：恢复资格，返还扣除的质押金
- 申诉失败：维持原判

### 6. 退出机制

#### 主动退出
```
1. 陪审员申请退出
2. 进入 7 天解锁期
3. 解锁期间：
   - 不再分配新任务
   - 必须完成已分配任务
   - 可以领取已获得的奖励
4. 7 天后：
   - 质押金全额返还
   - 积分清零
   - 失去陪审员资格
```

#### 被动退出
- 质押金低于 1,000 TAI（被惩罚扣除）
- 被永久取消资格
- 长期不活跃（90 天无投票）


## 🎨 DAO 界面设计

### 页面布局

```
┌─────────────────────────────────────────────────────────┐
│  DAO 陪审中心                                    [设置]   │
└─────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────┐
│  陪审员状态卡片                                          │
│  ┌─────────────┬─────────────┬─────────────┐           │
│  │ 质押金额     │ 当前积分     │ 投票准确率   │           │
│  │ 5,000 TAI   │ 156 pts     │ 87.5%       │           │
│  └─────────────┴─────────────┴─────────────┘           │
│                                                          │
│  抽签权重: 830,000  |  今日剩余抽签: 2/4                │
└─────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────┐
│  待处理任务 (3)                              [查看全部]   │
│  ┌───────────────────────────────────────────────────┐  │
│  │ 🔴 紧急  #12345 - "2024年比特币能否突破10万美元"   │  │
│  │ 剩余时间: 8小时 | 奖励: 2,000 TAI                  │  │
│  │ [查看详情] [立即投票]                              │  │
│  └───────────────────────────────────────────────────┘  │
│  ┌───────────────────────────────────────────────────┐  │
│  │ 🟡 标准  #12344 - "特斯拉Q4销量预测"              │  │
│  │ 剩余时间: 36小时 | 奖励: 1,500 TAI                │  │
│  │ [查看详情] [立即投票]                              │  │
│  └───────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────┐
│  收益统计                                                │
│  ┌─────────────┬─────────────┬─────────────┐           │
│  │ 待领取       │ 已领取       │ 总收益       │           │
│  │ 8,750 TAI   │ 45,200 TAI  │ 53,950 TAI  │           │
│  └─────────────┴─────────────┴─────────────┘           │
│  [一键领取]                                              │
└─────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────┐
│  历史记录                                    [筛选]       │
│  ┌───────────────────────────────────────────────────┐  │
│  │ ✅ #12340 - 已完成 | 多数派 | +2,000 TAI | +2 pts │  │
│  │ ✅ #12338 - 已完成 | 多数派 | +1,800 TAI | +2 pts │  │
│  │ ⏱️ #12335 - 已过期 | 未投票 | +0 TAI | +0 pts     │  │
│  └───────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────┘
```


### 核心组件设计

#### 1. JurorStatusCard - 陪审员状态卡片
```typescript
interface JurorStatus {
  staked: number;          // 质押金额
  points: number;          // 当前积分
  accuracy: number;        // 投票准确率 (0-100)
  weight: number;          // 抽签权重
  dailyDrawsLeft: number;  // 今日剩余抽签次数
  totalDraws: number;      // 今日总抽签次数
  isActive: boolean;       // 是否激活
  unlockDate?: string;     // 解锁日期（如果在解锁期）
}
```

**显示内容**：
- 质押金额（可点击追加质押）
- 当前积分
- 投票准确率（带颜色指示：>80% 绿色，60-80% 黄色，<60% 红色）
- 抽签权重计算公式提示
- 今日剩余抽签次数

**交互**：
- 点击质押金额 → 追加质押弹窗
- 点击积分 → 积分明细弹窗
- 点击准确率 → 历史投票记录


#### 2. JuryTaskList - 陪审任务列表
```typescript
interface JuryTask {
  id: string;
  marketId: string;
  title: string;
  description: string;
  type: 'dispute' | 'report' | 'timeout';
  priority: 'urgent' | 'normal';
  deadline: string;
  reward: number;
  status: 'pending' | 'in_progress' | 'voted' | 'completed' | 'expired';
  assignedAt: string;
  votedAt?: string;
  evidence?: {
    optionA: { votes: number; amount: number };
    optionB: { votes: number; amount: number };
    reports?: string[];
  };
}
```

**显示内容**：
- 任务优先级标识（🔴 紧急 / 🟡 标准）
- 任务 ID 和标题
- 剩余时间倒计时
- 预估奖励
- 任务状态

**交互**：
- [查看详情] → 打开任务详情页
- [立即投票] → 打开投票弹窗
- 点击任务卡片 → 展开更多信息


#### 3. VoteModal - 投票弹窗
```typescript
interface VoteModalProps {
  task: JuryTask;
  onVote: (choice: 'A' | 'B' | 'invalid', reason?: string) => Promise<void>;
  onClose: () => void;
}
```

**布局**：
```
┌─────────────────────────────────────────┐
│  投票 - #12345                    [关闭] │
├─────────────────────────────────────────┤
│  预测标题：2024年比特币能否突破10万美元  │
│  预测规则：[展开查看]                    │
│                                          │
│  投票数据：                              │
│  ┌─────────────┬─────────────┐          │
│  │ 选项 A: 能  │ 选项 B: 不能 │          │
│  │ 1,234 票    │ 987 票      │          │
│  │ 456,000 TAI │ 234,000 TAI │          │
│  └─────────────┴─────────────┘          │
│                                          │
│  举报理由（如有）：                      │
│  "创建者操纵投票..."                     │
│                                          │
│  你的投票：                              │
│  ○ 选项 A 胜出                          │
│  ○ 选项 B 胜出                          │
│  ○ 预测无效                             │
│                                          │
│  投票理由（可选）：                      │
│  [文本框]                                │
│                                          │
│  [取消] [确认投票]                       │
└─────────────────────────────────────────┘
```


#### 4. JurorEarningsCard - 收益统计卡片
```typescript
interface JurorEarnings {
  pending: number;      // 待领取
  claimed: number;      // 已领取
  total: number;        // 总收益
  breakdown: {
    base: number;       // 基础奖励
    bonus: number;      // 额外奖励
    accuracy: number;   // 准确率奖励
  };
}
```

**显示内容**：
- 待领取金额（大号显示）
- 已领取金额
- 总收益
- 收益明细（可展开）
- 一键领取按钮

**交互**：
- [一键领取] → 签名交易，领取所有待领取收益
- 点击金额 → 查看收益明细


#### 5. JurorHistoryList - 历史记录列表
```typescript
interface JuryHistory {
  taskId: string;
  marketTitle: string;
  votedAt: string;
  choice: 'A' | 'B' | 'invalid';
  result: 'majority' | 'minority' | 'pending';
  reward: number;
  pointsEarned: number;
  status: 'completed' | 'expired' | 'pending';
}
```

**显示内容**：
- 任务 ID 和标题
- 投票时间
- 投票选择
- 是否多数派
- 获得奖励
- 获得积分

**筛选选项**：
- 全部 / 已完成 / 进行中 / 已过期
- 多数派 / 少数派
- 时间范围


### 非陪审员视图

对于还不是陪审员的用户，显示：

```
┌─────────────────────────────────────────────────────────┐
│  成为陪审员                                              │
│                                                          │
│  ⚖️ 参与预测市场治理，获得丰厚奖励                      │
│                                                          │
│  ✅ 最低质押：1,000 TAI                                 │
│  ✅ 每次陪审：平均 2,000 TAI 奖励                       │
│  ✅ 积分系统：提升抽签权重                              │
│  ✅ 灵活退出：7 天解锁期                                │
│                                                          │
│  你的余额：5,234 TAI ✅                                 │
│                                                          │
│  [立即申请]                                              │
└─────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────┐
│  陪审员权益                                              │
│  • 参与预测结果仲裁                                      │
│  • 获得 1% 手续费分成                                   │
│  • 积分影响抽签权重                                      │
│  • 连续参与额外奖励                                      │
└─────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────┐
│  陪审员责任                                              │
│  • 公正投票，不得作弊                                    │
│  • 及时处理分配的任务                                    │
│  • 提供投票理由（推荐）                                  │
│  • 遵守社区规则                                          │
└─────────────────────────────────────────────────────────┘
```


## 🔔 通知系统

### Telegram Bot 通知

#### 任务分配通知
```
🎯 新陪审任务

任务 #12345
标题：2024年比特币能否突破10万美元
类型：争议预测
优先级：🔴 紧急
截止时间：8小时后
预估奖励：2,000 TAI

[查看详情] [立即投票]
```

#### 任务即将过期提醒
```
⏰ 任务即将过期

任务 #12345 还有 2 小时就要过期了！
请尽快完成投票，否则将失去奖励。

[立即投票]
```

#### 投票结果通知
```
✅ 投票结果已确定

任务 #12345 已完成
你的投票：选项 A
最终结果：选项 A（多数派）

奖励：+2,000 TAI
积分：+2 pts

[查看详情]
```

#### 收益到账通知
```
💰 收益已到账

陪审任务 #12345
奖励：2,000 TAI
当前待领取：8,750 TAI

[一键领取]
```


## 📊 数据库设计

### 新增表

#### jurors - 陪审员表
```sql
CREATE TABLE jurors (
  id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
  user_id UUID NOT NULL REFERENCES users(id),
  wallet_address TEXT NOT NULL,
  staked_amount BIGINT NOT NULL DEFAULT 0,
  points INTEGER NOT NULL DEFAULT 0,
  total_votes INTEGER NOT NULL DEFAULT 0,
  correct_votes INTEGER NOT NULL DEFAULT 0,
  accuracy DECIMAL(5,2) DEFAULT 0,
  status TEXT NOT NULL DEFAULT 'active', -- active, unlocking, inactive
  unlock_date TIMESTAMP,
  created_at TIMESTAMP DEFAULT NOW(),
  updated_at TIMESTAMP DEFAULT NOW(),
  UNIQUE(user_id)
);

CREATE INDEX idx_jurors_user_id ON jurors(user_id);
CREATE INDEX idx_jurors_status ON jurors(status);
CREATE INDEX idx_jurors_points ON jurors(points DESC);
```

#### jury_tasks - 陪审任务表
```sql
CREATE TABLE jury_tasks (
  id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
  market_id UUID NOT NULL REFERENCES markets(id),
  type TEXT NOT NULL, -- dispute, report, timeout
  priority TEXT NOT NULL DEFAULT 'normal', -- urgent, normal
  deadline TIMESTAMP NOT NULL,
  reward BIGINT NOT NULL,
  status TEXT NOT NULL DEFAULT 'open', -- open, voting, completed, expired
  result TEXT, -- A, B, invalid
  created_at TIMESTAMP DEFAULT NOW(),
  completed_at TIMESTAMP
);

CREATE INDEX idx_jury_tasks_market_id ON jury_tasks(market_id);
CREATE INDEX idx_jury_tasks_status ON jury_tasks(status);
CREATE INDEX idx_jury_tasks_deadline ON jury_tasks(deadline);
```


#### jury_assignments - 任务分配表
```sql
CREATE TABLE jury_assignments (
  id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
  task_id UUID NOT NULL REFERENCES jury_tasks(id),
  juror_id UUID NOT NULL REFERENCES jurors(id),
  assigned_at TIMESTAMP DEFAULT NOW(),
  status TEXT NOT NULL DEFAULT 'pending', -- pending, in_progress, voted, expired
  vote TEXT, -- A, B, invalid
  vote_reason TEXT,
  voted_at TIMESTAMP,
  reward BIGINT DEFAULT 0,
  points_earned INTEGER DEFAULT 0,
  UNIQUE(task_id, juror_id)
);

CREATE INDEX idx_jury_assignments_task_id ON jury_assignments(task_id);
CREATE INDEX idx_jury_assignments_juror_id ON jury_assignments(juror_id);
CREATE INDEX idx_jury_assignments_status ON jury_assignments(status);
```

#### jury_draws - 抽签记录表
```sql
CREATE TABLE jury_draws (
  id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
  juror_id UUID NOT NULL REFERENCES jurors(id),
  draw_time TIMESTAMP NOT NULL,
  weight BIGINT NOT NULL,
  selected BOOLEAN DEFAULT FALSE,
  task_id UUID REFERENCES jury_tasks(id),
  created_at TIMESTAMP DEFAULT NOW()
);

CREATE INDEX idx_jury_draws_juror_id ON jury_draws(juror_id);
CREATE INDEX idx_jury_draws_draw_time ON jury_draws(draw_time);
```


## 🔧 API 设计

### 陪审员相关

#### POST /api/juror/apply
申请成为陪审员
```typescript
Request: {
  userId: string;
  stakeAmount: number; // 质押金额
  txHash: string;      // 交易哈希
}

Response: {
  success: boolean;
  juror: JurorStatus;
}
```

#### GET /api/juror/status/:userId
获取陪审员状态
```typescript
Response: JurorStatus
```

#### POST /api/juror/stake
追加质押
```typescript
Request: {
  userId: string;
  amount: number;
  txHash: string;
}

Response: {
  success: boolean;
  newStake: number;
}
```

#### POST /api/juror/unstake
申请退出（开始解锁期）
```typescript
Request: {
  userId: string;
}

Response: {
  success: boolean;
  unlockDate: string;
}
```


### 任务相关

#### GET /api/jury/tasks/:jurorId
获取陪审员的任务列表
```typescript
Query: {
  status?: 'pending' | 'in_progress' | 'voted' | 'completed' | 'expired';
  limit?: number;
  offset?: number;
}

Response: {
  tasks: JuryTask[];
  total: number;
}
```

#### GET /api/jury/task/:taskId
获取任务详情
```typescript
Response: {
  task: JuryTask;
  market: MarketDetails;
  evidence: TaskEvidence;
}
```

#### POST /api/jury/vote
提交投票
```typescript
Request: {
  taskId: string;
  jurorId: string;
  vote: 'A' | 'B' | 'invalid';
  reason?: string;
}

Response: {
  success: boolean;
  pointsEarned: number;
  rewardEarned: number;
}
```

#### GET /api/jury/history/:jurorId
获取投票历史
```typescript
Query: {
  status?: string;
  result?: 'majority' | 'minority';
  startDate?: string;
  endDate?: string;
  limit?: number;
  offset?: number;
}

Response: {
  history: JuryHistory[];
  total: number;
  stats: {
    totalVotes: number;
    correctVotes: number;
    accuracy: number;
    totalRewards: number;
  };
}
```


## 🎮 实施优先级

### Phase 1: 核心功能（MVP）
- [ ] 陪审员申请和质押
- [ ] 任务分配系统（手动触发）
- [ ] 投票功能
- [ ] 基础奖励发放
- [ ] 积分系统
- [ ] DAO 界面基础版

### Phase 2: 自动化
- [ ] 自动抽签系统（定时任务）
- [ ] Telegram Bot 通知
- [ ] 任务过期处理
- [ ] 收益自动结算

### Phase 3: 高级功能
- [ ] 惩罚机制
- [ ] 申诉系统
- [ ] 高级统计和分析
- [ ] 陪审员排行榜
- [ ] 投票理由展示

### Phase 4: 优化
- [ ] 权重算法优化
- [ ] 反作弊机制
- [ ] 性能优化
- [ ] 移动端优化


## 💡 关键设计考虑

### 1. 防止作弊
- 抽签算法使用链上随机数（VRF）
- 投票结果加密存储，统一揭晓
- 异常投票模式检测
- 社区举报机制

### 2. 激励平衡
- 基础奖励保证参与动力
- 积分系统鼓励长期贡献
- 准确率影响权重，但不过度惩罚
- 快速响应奖励提高效率

### 3. 用户体验
- 清晰的任务状态和倒计时
- 简化的投票流程
- 及时的通知提醒
- 透明的奖励计算

### 4. 可扩展性
- 支持不同类型的任务
- 灵活的奖励规则
- 可配置的惩罚机制
- 模块化的组件设计

---

**文档版本**：v1.0  
**创建时间**：2025-11-03  
**状态**：设计阶段
